<UserControl x:Class="Tradentum.Views.Page1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Tradentum.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Встроенный конвертер для ProgressBar -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid >
        <!-- 2 ГЛАВНЫЕ КОЛОНКИ -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= ЛЕВАЯ КОЛОНКА: 📺 ПЛЕЕР ================= -->
        <Border Grid.Column="0">
            <Grid VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#1E1E2E" CornerRadius="8,8,0,0">
                    <TextBlock Text="📺 Прямой эфир" FontSize="13" Foreground="White" FontWeight="Medium" HorizontalAlignment="Center" Padding="0,8"/>
                </Border>
                <Border Grid.Row="1" Background="#000000" CornerRadius="4" ClipToBounds="True">
                    <wv2:WebView2 x:Name="RutubePlayer" Source="https://rutube.ru/" Width="640" Height="360" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- ================= ПРАВАЯ КОЛОНКА: ТАБЛИЦЫ ================= -->
        <Border Grid.Column="1">
            <TabControl Background="{DynamicResource SecondaryBackground}" 
            BorderBrush="{DynamicResource BorderBrush}" 
            BorderThickness="1">

                <!-- 📊 Топ ликвидности -->
                <TabItem Header="📊 Ликвидность">
                    <DataGrid ItemsSource="{Binding TopSecurities}" 
              AutoGenerateColumns="False" 
              IsReadOnly="True" 
              CanUserAddRows="False" 
              CanUserDeleteRows="False"
              SelectionMode="Single"
              GridLinesVisibility="Horizontal"
              Background="{DynamicResource SecondaryBackground}"
              AlternatingRowBackground="{DynamicResource RowBackground}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Тикер" Binding="{Binding SecCode}" Width="80" />
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Цена" Binding="{Binding PriceDisplay}" Width="100" />
                            <DataGridTextColumn Header="Δ Абс." Binding="{Binding ChangeDisplay}" Width="90">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}" />
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Δ %" Binding="{Binding ChangePercentDisplay}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}" />
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Объём" Binding="{Binding TurnoverDisplay}" Width="120" />
                            <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdateDisplay}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <!-- 📈 Лидеры роста -->
                <TabItem Header="📈 Рост">
                    <DataGrid ItemsSource="{Binding TopGainers}" 
              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Тикер" Binding="{Binding SecCode}" Width="80" />
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Цена" Binding="{Binding PriceDisplay}" Width="100" />
                            <DataGridTextColumn Header="Рост %" Binding="{Binding ChangePercentDisplay}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="LimeGreen" />
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <!-- 📉 Лидеры падения -->
                <TabItem Header="📉 Падение">
                    <DataGrid ItemsSource="{Binding TopLosers}" 
              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Тикер" Binding="{Binding SecCode}" Width="80" />
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Цена" Binding="{Binding PriceDisplay}" Width="100" />
                            <DataGridTextColumn Header="Падение %" Binding="{Binding ChangePercentDisplay}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Tomato" />
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

            </TabControl>
        </Border>
    </Grid>
</UserControl>